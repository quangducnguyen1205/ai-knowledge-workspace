spring:
  application:
    name: workspace-core

server:
  port: ${SERVER_PORT:8080}

integration:
  fastapi:
    base-url: ${FASTAPI_BASE_URL:http://localhost:8000}
    connect-timeout: ${FASTAPI_CONNECT_TIMEOUT:5s}
    read-timeout: ${FASTAPI_READ_TIMEOUT:30s}
