package com.aiknowledgeworkspace.workspacecore.processing.api;

/** Persisted processing lifecycle values exposed as an application fact. */
public enum ProcessingJobStatus {
    PENDING,
    RUNNING,
    SUCCEEDED,
    FAILED
}
